


## clone the repo
```bash
-> git clone repo
-> cd repo
```
## To run the code install the requirements
```bash
uv sync
```

## execute the the preprocessing
```python
uv run data_processing/map_sample_to_physicianlist.py
```
## then go to the image_preprocessing.ipynb
execute from the format fintune data as the ground_truth.jsonl is already saved in drive

